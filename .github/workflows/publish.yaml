name: publish

on:
  push:
    tags: ["v[0-9]+.[0-9]+.[0-9]+*"]

jobs:
  review: {uses: aprosail/templates/.github/workflows/dart-review.yaml@main}
  publish: {uses: aprosail/templates/.github/workflows/dart-publish.yaml@main}
